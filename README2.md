Gitflow is a strict branching model for Git that uses specific branches to manage complex software releases with 
scheduled cycles, separating production code from ongoing development for better control and stability
